{
  "name": "jsonserver",
  "version": "1.0.0",
  "description": "This is a simple json server example used in https://github.com/darpanrangari/react-native-blog-crud",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "db": "json-server -w db.json",
    "tunnel": "ngrok http 3000"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.15.0",
    "ngrok": "^3.2.5"
  }
}
